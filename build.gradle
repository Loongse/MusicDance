// Top-level build file where you can add configuration options common to all sub-projects/modules.
apply from: this.rootProject.file('md.gradle')
buildscript {
    repositories {
        google()
        jcenter()
        mavenCentral()
        maven {
            url 'http://192.168.0.29:8081/repository/md_release/'
            credentials {
                username 'admin'
                password '123456'
            }
        }
        maven {
            url 'http://192.168.0.29:8081/repository/md_snapshots/'
//            url 'http://localhost:8081/repository/md_release/'
            credentials {
                username 'admin'
                password '123456'
            }
        }
    }
    dependencies {
        classpath "com.android.tools.build:gradle:4.1.1"

        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
        //greendao插件依赖
        classpath 'org.greenrobot:greendao-gradle-plugin:3.2.2'
    }
}

allprojects {
    repositories {
        google()
        jcenter()
        mavenCentral()
        maven{
            url 'https://jitpack.io'
        }
        maven {
            url 'http://192.168.0.29:8081/repository/md_release/'
            credentials {
                username 'admin'
                password '123456'
            }
        }
        maven {
            url 'http://192.168.0.29:8081/repository/md_snapshots/'
            credentials {
                username 'admin'
                password '123456'
            }
        }
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}